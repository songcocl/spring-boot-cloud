server:
  port: 8769

management:
  security:
    enabled: false

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000

zuul:
  ignoredServices: '*'
  routes:
    amcloud-authservice:
      path: /uaa/**
      stripPrefix: false
      sensitiveHeaders:
    amcloud-admin:
      path: /api/admin/**
      sensitiveHeaders: 
    amcloud-api: 
      path: /api/**
      sensitiveHeaders: 
